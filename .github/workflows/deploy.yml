name: Deploy to Xneelo

on:
  push:
    branches: [main]
  workflow_dispatch: {}

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install lftp
        run: sudo apt-get update -y && sudo apt-get install -y lftp

      # lftp's --exclude-glob patterns turned out NOT to reliably keep
      # .git/.github out of the upload (verified live on 2026-09-08: both
      # ended up as real directories in the public web root on two
      # separate deploys, despite --exclude-glob .git* below). Deleting
      # them from the runner's own checkout first is foolproof — there's
      # nothing left to upload — and only affects this ephemeral runner
      # copy, not the actual repo/remote.
      - name: Strip VCS metadata from the checkout before it can be uploaded
        run: rm -rf .git .github

      # Additive-only deploy: uploads new/changed files, overwrites files
      # that changed, and NEVER deletes anything on the server (no --delete
      # flag). config/config.php and uploads/* are gitignored, so they're
      # simply absent from this checkout and are never touched by this
      # step — the live database password, Paystack/Gemini keys, and every
      # candidate's uploaded resume/SLA/logo stay exactly where they are
      # across every deploy.
      #
      # IMPORTANT: this FTP account's public_html hosts the ENTIRE main
      # rvzgroup.co.za corporate site (portal, payments, blog, shop,
      # vendor-portal, divisions, etc.) as siblings of this app's own
      # recruitment.rvzgroup.co.za subfolder. The target path below MUST
      # stay scoped to that one subfolder — deploying to bare /public_html/
      # would overwrite an entirely different, unrelated live application.
      #
      # NOTE: this was originally deployed to the misspelled
      # recruitement.rvzgroup.co.za subfolder (2026-09-08). Repointed to
      # the correctly-spelled recruitment.rvzgroup.co.za the same day,
      # after discovering the correct-spelling subdomain was still an
      # unused cPanel placeholder. The old misspelled subfolder now just
      # holds a redirect (see .htaccess) — see README.
      - name: Upload to recruitment.rvzgroup.co.za (never deletes, never touches sibling folders)
        env:
          FTP_SERVER: ${{ secrets.FTP_SERVER }}
          FTP_USERNAME: ${{ secrets.FTP_USERNAME }}
          FTP_PASSWORD: ${{ secrets.FTP_PASSWORD }}
        run: |
          lftp -u "$FTP_USERNAME","$FTP_PASSWORD" "$FTP_SERVER" <<'LFTP_EOF'
          set ftp:ssl-allow no
          set mirror:parallel-transfer-count 4
          mirror --reverse --verbose \
            --exclude-glob .gitignore \
            ./ /public_html/recruitment.rvzgroup.co.za/
          LFTP_EOF

      - name: Verify site is responding
        run: |
          curl -sf -o /dev/null -w "Site responded: %{http_code}\n" "https://recruitment.rvzgroup.co.za/index.php"
