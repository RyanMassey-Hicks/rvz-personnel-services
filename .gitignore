# Real secrets — DB password, live Paystack/Gemini/SMTP/encryption keys.
# Never commit this. config.sample.php is the tracked template.
/config/config.php

# User-uploaded content (resumes, SLAs, logos, generated ads) — real
# candidate/company data, not app source. Kept on the live server only.
/uploads/*
!/uploads/.htaccess

# OS/editor cruft
.DS_Store
Thumbs.db
*.swp
